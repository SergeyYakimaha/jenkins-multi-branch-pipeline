pipeline {
    agent {
        label 'Vision1'
    }
    stages {
        stage("First step: dev_branch") {
            steps {
                bat('java -version')
                bat('java -version')
            }
        }
        stage("Second step: dev_branch") {
            steps {
                bat('java -version')
                bat('java -version')
            }
        }
    }
}